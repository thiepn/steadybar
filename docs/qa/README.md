# Steadybar 1.2 verification evidence

These files were produced during the Steadybar rename run. `ui-results.json` records rendered tests with memory storage; `e2e-results.json` records the blocked real-origin probe. Do not confuse those modes. The old 1.1 evidence remains in the original music-practice-os-v1.1.zip, not this directory.

`rendered-browser-tests.txt` includes the 13-route, six-viewport matrix and 26 passing workflows. Three persistence/offline-only checks were skipped in rendered mode. The native suite is unchanged and enabled for real-origin CI.
